# AUTO-GENERATED by scripts/make_delta_configs.py -- do not hand-edit.
# Regenerate with: python scripts/make_delta_configs.py
#
# INTENSITY-HEAD BAKE-OFF, arm INDEP.   model.head_parameterisation = independent
#
# The two arms are IDENTICAL except for `model.head_parameterisation`. That IS the
# experiment. Motivation + diagnosis: docs/shrinkage_probe_findings.md and
# docs/intensity_head_fix_proposal.md. The state_dict is identical between modes, so
# both arms have the same parameter count; only the interpretation of the two
# intensity output channels differs (independent: logI1,logI2; delta: logI1,delta).
#
# Short schedule (8000 steps, curriculum ramp 4000). Diagnostic, not a headline.
#
# Read out with (compare the arms to EACH OTHER, never to the 40k model):
#   python scripts/bright_dense_probe.py --checkpoint <run-dir> --device cuda
#   python scripts/shrinkage_probe.py    --checkpoint <run-dir>
experiment:
  name: headfix-INDEP
  note: 'Intensity-head bake-off, arm INDEP: head_parameterisation=independent. Diagnostic only (8000
    steps). Tests the (logI1, delta) fix for the protein-under-crowding shrinkage. See docs/intensity_head_fix_proposal.md.'
seed: 0
model:
  in_channels: 2
  base_channels: 32
  num_branches: 3
  blocks_per_branch: 2
  head_mid_channels: 32
  heatmap_bias: -2.19
  head_parameterisation: independent
simulator:
  image:
    height: 256
    width: 256
  detector:
    n_frames: 3
    poisson_gaussian_threshold: 20
    adc_max: 4095
    ch1:
      gain_range:
      - 3
      - 30
      offset: 154.0
      read_var: 3.1
      saturation_knee: 3941.0
      excess_noise_factor: 1.0
    ch2:
      gain_range:
      - 20
      - 150
      offset: 154.0
      read_var: 4.4
      saturation_knee: 3941.0
      excess_noise_factor: 1.0
  scene:
    density:
      min: 0.0006
      max: 0.03
    oversample_dense_fraction: 0.3
    ratio_law:
      alpha:
        min: -0.7
        max: 0.7
      beta:
        min: -0.6
        max: 0.6
      scatter_std:
        min: 0.03
        max: 0.25
    psf:
      sigma1:
        min: 1.0
        max: 1.8
      c2_sigma_mismatch:
        min: 1.05
        max: 1.35
    registration_shift:
      max_px: 1.0
    background:
      level:
        min: 1.0
        max: 4.0
      gradient_frac:
        min: 0.0
        max: 0.0
      structure_frac:
        min: 0.0
        max: 0.0
    clustering:
      cluster_prob: 0.4
      n_clusters:
        min: 2
        max: 8
      cluster_sigma_px:
        min: 6.0
        max: 24.0
training:
  train_steps: 8000
  batch_size: 16
  lr: 0.002
  weight_decay: 0.0001
  grad_clip_norm: 1.0
  heatmap_sigma: 1.5
  lr_warmup_steps: 300
  variance_warmup_steps: 800
  curriculum:
    enabled: true
    curriculum_ramp_steps: 4000
  logvar_min: -10.0
  logvar_max: 6.0
  loss_weights:
    heatmap: 1.0
    offset: 1.0
    intensity: 1.0
  intensity_window:
    full_decades: 2.0
    full_dim_bias: 1.6
    floor_a1_photons: 3.0
    target_frac: 0.85
    scatter_sigmas: 3.5
  eval_every: 500
  checkpoint_every: 1000
  log_every: 100
  best_checkpoint:
    enabled: true
    metric: val_logratio_mae
    hard_corner_min_pairs: 50
  val:
    seed: 12345
    n_images: 24
    n_hard_corner: 10
benchmark:
  match_radius_px: 3.0
  match_method: greedy
  snr_bins:
  - 0.0
  - 0.75
  - 1.25
  - 2.0
  - 3.0
  - 5.0
  - .inf
  density_bins:
  - 0.0
  - 1.0
  - 3.0
  - 6.0
  - .inf
  density_radius_px: 4.0
  our_model:
    peak_threshold: 0.3
    nms_kernel: 3
    max_spots: 3000
