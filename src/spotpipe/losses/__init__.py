"""Training losses (STUB package -- implemented in build stage 3).

Phase 1 has NO slope loss: the ratio-law slope beta is computed explicitly
downstream of inference, never trained on. See CLAUDE.md.
"""
