"""Benchmark metrics (STUB -- implemented in build stage 4).

Bias/variance of per-spot intensities and downstream slope, measured on the
fixed evaluation set -- with particular attention to the dim x high-overlap
corner. See CLAUDE.md.
"""

from __future__ import annotations


def compute_metrics(*args, **kwargs):
    """Compute benchmark metrics from canonical-schema outputs. STUB."""
    raise NotImplementedError("metrics.compute_metrics is implemented in build stage 4.")
