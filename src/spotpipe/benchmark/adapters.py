"""Method adapters (STUB -- implemented in build stage 4).

Each adapter wraps a spot-detection method (our model or an external baseline)
and emits the canonical schema in :mod:`spotpipe.schema`, identically.
"""

from __future__ import annotations


def get_adapter(*args, **kwargs):
    """Return a method adapter by name. STUB."""
    raise NotImplementedError("adapters.get_adapter is implemented in build stage 4.")
