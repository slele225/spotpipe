"""Benchmark harness and method adapters (STUB package -- build stage 4).

Every spot-detection method (our model and external baselines) is wrapped by an
adapter that emits the canonical schema in :mod:`spotpipe.schema`, so the
harness and metrics are method-agnostic.
"""
