"""Ground-truth <-> prediction matching (STUB -- later build stage).

Used to match predicted spots to ground-truth spots (e.g. for intensity-loss
masking and benchmark metrics).
"""

from __future__ import annotations


def match_spots(*args, **kwargs):
    """Match predicted spots to ground-truth spots. STUB."""
    raise NotImplementedError("matching.match_spots is implemented in a later build stage.")
